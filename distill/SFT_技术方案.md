# SFT 技术方案手册 —— Planner 决策蒸馏

> 面向理解与面试讲述。配套的操作步骤见 `SFT_训练操作手册.pdf`；本文档讲的是「为什么这样设计」，帮你把每个技术决策讲透。
>
> 真实结果锚点：Qwen2.5-1.5B-Instruct + QLoRA，540 条训练 / 60 条验证，3 epoch，约 2 分钟，**val 合法率 100%、route 精确匹配 96.67%**。

---

## 0. 一句话讲清这件事

我们有一个线上的「路由规划器（route planner）」，它把用户问题拆解成一段受控的工具调用计划（JSON）。线上可以用 DeepSeek 这样的云端大模型来做这件事，但它**要联网、有延迟、有成本**。

**SFT 蒸馏做的事**：把「问题 → 合法计划」这个能力，用监督微调（Supervised Fine-Tuning）教给一个只有 1.5B、能在本地/边缘跑的小模型。教完之后，小模型对验证集 100% 输出格式合法、可被系统解析的计划——等于用几十 MB 的 LoRA 权重，换掉了对云端大模型的依赖。

这就是「知识蒸馏（knowledge distillation）」在 LLM 上的一种落地：**大模型/人类专家当老师，小模型当学生，学生模仿老师的决策**。

---

## 1. 任务定位：这是哪一类微调？

微调不是只有一种。先把坐标系讲清楚，面试时能体现你知道自己在做什么：

| 维度 | 本方案的选择 | 为什么 |
| --- | --- | --- |
| 微调类型 | **SFT（监督微调）** | 有明确的「输入→正确输出」配对，属于模仿学习，不是 RLHF/DPO 那种偏好优化 |
| 训练目标 | **结构化生成**（输出受限 JSON） | 不是开放对话，是「把自然语言映射到一个受 schema 约束的计划」 |
| 参数更新方式 | **LoRA（参数高效微调 PEFT）** | 不动原模型权重，只训练一小撮低秩矩阵，省显存、产物小、可插拔 |
| 精度 | **QLoRA（4-bit 量化 + LoRA）** | 把基座压成 4-bit 装进显存，单卡 12–16G 就能跑 1.5B |

**面试一句话**：这是一个「用 QLoRA 做参数高效 SFT，把云端 planner 的结构化决策能力蒸馏到本地小模型」的任务。

---

## 2. 核心概念逐个拆开讲

### 2.1 SFT（监督微调）到底在优化什么

语言模型的本质是「预测下一个 token」。SFT 就是拿「输入 + 标准答案」的配对数据，让模型在给定输入时，把标准答案的每个 token 的预测概率拉高。损失函数就是标准的**交叉熵（cross-entropy）语言建模损失**——预测对了 loss 低，预测错了 loss 高。

你训练日志里的 `loss` 从 0.60 一路降到 0.058，就是这个交叉熵在下降：模型越来越会「照着标准计划把 token 接出来」。

### 2.2 为什么用 LoRA 而不是全参数微调

全参数微调（full fine-tuning）会更新模型**所有**权重。1.5B 模型全参微调，光是存优化器状态（Adam 要存动量和方差）就要好几倍模型大小的显存，小卡根本装不下。

**LoRA（Low-Rank Adaptation）的核心想法**：不直接改原权重矩阵 W，而是在旁边加一个「低秩旁路」——两个小矩阵 A（d×r）和 B（r×d），用它们的乘积 BA 来表示权重的增量 ΔW。前向时输出 = Wx + BAx。训练时**冻结 W，只训练 A 和 B**。

- `r`（秩，本方案设 16）：控制旁路的「表达能力」，越大能学的越多但参数也越多。
- `lora_alpha`（本方案 32）：缩放因子，实际增量按 alpha/r 缩放，相当于旁路的「学习强度」。
- **为什么有效**：微调对权重的改动通常是「低秩」的——不需要全维度的自由度就能把一个新任务学好。所以用一个低秩矩阵去逼近 ΔW，效果接近全参微调，但可训练参数量降到原来的百分之一甚至更低。
- **产物只有几十 MB**：因为只存 A、B 这两个小矩阵（adapter），推理时可以「即插即用」地叠加到原模型上。

**面试追问「LoRA 加在哪些层」**：本方案的 `lora_targets` 是 `q_proj, k_proj, v_proj, o_proj`（注意力的四个投影）+ `gate_proj, up_proj, down_proj`（MLP 的三个投影）。这是 Llama/Qwen 系列 decoder 的标准注入点——注意力和 FFN 都覆盖到，是社区验证过的稳妥配置。

### 2.3 为什么用 QLoRA（4-bit 量化）

QLoRA = **量化的基座 + LoRA**。把冻结的基座权重从 16-bit 压成 **4-bit（nf4 格式）**装进显存，只有旁路的 LoRA 参数保持较高精度参与训练。

- **省显存**：1.5B 的 16-bit 权重约 3GB，压成 4-bit 后不到 1GB，加上激活和 LoRA，单张 16G 卡绰绰有余。
- **精度损失可控**：基座只是「被查询」不参与梯度更新，量化误差影响小；真正学习的 LoRA 参数是高精度的。
- 代码里对应 `BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type="nf4", bnb_4bit_use_double_quant=True)`。`nf4` 是专为神经网络权重分布设计的 4-bit 格式，`double_quant` 再对量化常数本身做一次量化，进一步省内存。

**面试一句话**：QLoRA 让我在一张消费级卡上微调 1.5B，把基座 4-bit 冻结、只训练高精度 LoRA 旁路，兼顾显存和效果。

### 2.4 completion-only loss：只对「答案」算损失

一条训练样本包含 `prompt`（system + user 消息，即那段固定的工具说明 + 用户问题）和 `completion`（模型该输出的计划 JSON）。

如果对整条序列都算 loss，模型会浪费容量去「学着复述那段固定的系统提示」——但那段提示推理时是给定的，不需要模型生成。所以我们用 `SFTConfig(completion_only_loss=True)`，**把 prompt 部分的 loss 掩掉（mask），只对 completion 部分算交叉熵**。

- 好处：训练信号集中在「真正要学的东西」——即给定问题该产出什么计划。
- 实现变迁（值得一提，显示你踩过真实的坑）：旧版 TRL 用 `DataCollatorForCompletionOnlyLM` 靠字符串标记定位答案起点；TRL 1.7.1 把它内置成了 `completion_only_loss=True`，配合 prompt/completion 两字段数据格式自动完成掩码。

### 2.5 chat template 对齐：训练格式 = 推理格式

这是一个容易被忽略、但**做错就前功尽弃**的点。

Qwen 这类 instruct 模型，对话要按它约定的模板拼接（比如用 `<|im_start|>role ... <|im_end|>` 包裹每一轮）。**训练时喂给模型的格式，必须和推理时线上构造 prompt 的格式一模一样**，否则模型学到的是 A 格式、线上喂 B 格式，行为会漂移。

本方案的做法：训练数据的 prompt 直接复用线上 planner 的 `build_planner_messages()` 生成的消息结构，再由 SFTTrainer 用**模型自己的 chat template**（`tokenizer.apply_chat_template`）渲染。这样「训什么格式」和「线上跑什么格式」由同一套代码保证一致。

**面试一句话**：我把训练数据的 prompt 构造和线上推理的 prompt 构造统一到同一个函数，避免 train/inference 格式漂移——这是蒸馏能真正上线的前提。

---

## 3. 数据方案：为什么这批数据能撑起 100% 合法率

模型效果的上限由数据决定。这部分是整个方案里最花功夫、也最能体现工程能力的地方。

### 3.1 数据从哪来：三层来源

1. **手工精标（gold）600 条**：`{question, steps}`，人工标注每个问题对应的正确计划。这是最高质量的核心集。
2. 问题扩增：`augment_questions.py` 对种子问题做改写/组合，扩大问题多样性。
3. 教师标注：`build_sft_data.py` 用线上 planner 当老师批量产出标签（本方案主要用 gold 集训练）。

### 3.2 关键设计：每条标签都过「线上同一个 guard」

这是本方案区别于「随便凑个数据集」的核心。所有标签在进训练集前，都用线上系统真实使用的 `validate_plan_steps()` 校验一遍——route 是否合法、工具是否属于该 route、policy 步是否在最后、时间窗格式是否支持……不合法的直接拒绝。

- 结果：**600/600 全部通过**，训练集零脏数据。
- 意义：模型学的每一个样本都是「线上系统认可的合法计划」，所以学出来的模型输出合法率自然高。100% 的 val legality 不是运气，是数据质量的直接结果。
- 这一步还在标注过程中抓出过真实错误（比如把 `last_7_days` 写成了不被支持的时间窗），guard 直接拦下——**校验器既保证了训练数据，也充当了标注的质检**。

### 3.3 数据分布：为什么刻意提高多步占比

最终 600 条：1步 43% / 2步 28% / 3步 22% / 4步 5%，多步（≥2）占 57%。

- 单步任务（「查一下温度」）路由简单，模型容易学；**多步复合任务**（「先查温度，检测异常，再给策略」）才是真正考验规划能力的地方。
- 所以我们刻意把多步占比从最初的 29% 提到 57%，让模型多见「拆解 + 排序 + policy 放最后」这类结构。
- 覆盖度：11 个工具全覆盖、14 个指标名都是真实数据列、中英文 + 口语化 + 抗干扰 + 多轮指代都有——保证模型不是只会背几个模板。

### 3.4 train/val 划分的纪律

540 训 / 60 验，划分时**保证问题不重复、train/val 无泄漏**（否则验证集分数虚高，骗自己）。曾经查出 1 条重复问题（gold_0479）并修掉。这种「不自欺」的细节，面试官会认。

---

## 4. 超参数：每个数字为什么这么设

| 超参 | 值 | 理由 |
| --- | --- | --- |
| epochs | 3 | 小数据集（540 条）+ 窄任务，3 轮足够收敛；再多易过拟合。eval_loss 到第 3 轮已平稳，验证了这个判断 |
| learning_rate | 2e-4 | LoRA 的典型学习率（比全参微调的 ~2e-5 大一个量级，因为只训练小旁路，可以更激进） |
| batch_size × grad_accum | 4 × 4 = 16 | 单步显存装 4 条，用梯度累积攒到等效 batch 16，兼顾显存和稳定性 |
| max_length | 1024 | 覆盖最长的「工具说明 + 多步计划」样本，又不浪费显存 |
| lora_r / alpha | 16 / 32 | r=16 对这种窄任务够用；alpha=2r 是常见缺省，缩放稳定 |
| lora_dropout | 0.05 | 轻微正则，防小数据过拟合 |
| scheduler | cosine + warmup 0.03 | 先小幅热身再余弦退火，训练后期学习率平滑趋零（日志里 lr 从 1.99e-4 降到 4.6e-7 可见） |

**面试追问「怎么判断没过拟合」**：看 **eval_loss 是否随训练下降且不反弹**。本方案 eval_loss 0.144→0.109→0.108 单调下降后收敛，train_loss 也没有远低于 eval_loss 的背离——所以没有过拟合。

---

## 5. 评测方案：为什么这个验收指标有说服力

很多人训完只看 loss，但 loss 低不代表「输出真的能用」。本方案的验收指标直接复用**线上代码**：

- 训练后，对 60 条验证问题让模型**实际生成**计划，再用线上 planner 的 `_decision_from_llm_payload()`（就是线上把 LLM 输出解析成 PlanDecision 的那个函数）去解析 + 校验。
- **legal_rate（合法率）= 100%**：60 条生成全部能被线上解析器接受。这等价于说「这个小模型的输出，线上系统能直接用」。
- **exact_match（route 精确匹配）= 96.67%**：58/60 的路由序列和人工标准答案完全一致。

**为什么比只看 loss 强**：这是「端到端、用生产同一套判据」的评测，而不是训练内部的代理指标。它回答的是「模型能不能真的替代线上 planner」，而不是「模型拟合得好不好」。

**面试一句话**：我的验收指标不是 loss，而是「生成的计划能否被线上系统解析并通过 guard」，用的是和生产完全相同的校验代码——所以 100% 合法率是可直接上线的信号，不是自定义的宽松指标。

---

## 6. 一张图串起整条链路

```
问题(自然语言)
   │
   │  build_planner_messages()   ← 训练/推理共用，保证格式一致
   ▼
[system: 工具schema说明] + [user: 问题]        ← prompt（loss 掩掉）
   │
   ▼   SFT 学的就是这一步的映射
[assistant: {"steps":[...], "confidence":...}]  ← completion（只对它算 loss）
   │
   │  _decision_from_llm_payload()  ← 线上解析器，也是验收用的判据
   ▼
PlanDecision（经 validate_plan_steps 校验合法）
```

训练数据的标签、训练时的 prompt、验收时的解析——**三处都复用线上同一套函数**。这是「蒸馏出来的模型能真正接回系统」的根本保证，也是这个方案最值得讲的工程亮点。

---

## 7. 局限与诚实的边界（面试主动说，加分）

- **exact_match 96.67% 不是 100%**：有 2 条路由和人工标注不同。需要人工看这 2 条是模型错了、还是标注本身有多解——不能默认模型错。
- **只在 60 条 val 上验证**：样本量不大，合法率 100% 说明「格式和路由学得好」，但要断言泛化能力，还需要更大的独立测试集，或接回阶段 4 的完整 benchmark 做三方对比（deterministic vs 云端教师 vs 蒸馏模型）。
- **SFT 只学了「模仿」**：它学的是「像老师那样路由」，没有「偏好优化」。如果要让模型在多个合法计划中更偏向某种（比如更少步数、更稳的工具选择），那是阶段 3 DPO 要做的事。
- **蒸馏上限 = 教师/标注上限**：学生再好也很难超过老师。要突破，得改进数据或引入偏好信号。

---

## 8. 术语速查（面试前扫一眼）

- **SFT**：Supervised Fine-Tuning，用「输入→标准答案」配对做监督微调，本质是模仿。
- **蒸馏（distillation）**：让小模型模仿大模型/专家的行为，把能力「压缩」进小模型。
- **LoRA**：低秩适配，冻结原权重、只训练低秩旁路矩阵，参数高效。
- **QLoRA**：基座 4-bit 量化 + LoRA，进一步省显存。
- **PEFT**：Parameter-Efficient Fine-Tuning，参数高效微调的统称，LoRA 是其代表。
- **completion-only loss**：只对答案部分算损失，不学固定的提示部分。
- **chat template**：instruct 模型约定的对话拼接格式，训练/推理必须一致。
- **交叉熵损失**：语言建模的标准损失，衡量「预测的 token 分布」和「真实 token」的差距。
- **过拟合判据**：看 eval_loss 是否随训练反弹、train/eval loss 是否背离。
